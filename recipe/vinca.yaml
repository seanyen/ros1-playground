ros_distro: noetic

# mapping for package keys
conda_index:
  - conda-forge.yaml
  - packages-ignore.yaml

packages_skip_by_deps:
  - ros_comm
  - roscpp_tutorials

packages_select_by_deps:
  - python_qt_binding

patch_dir: patch
