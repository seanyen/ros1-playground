ros_distro: noetic
ros_python_version: 3

repos: noetic.2020.03.07.rosinstall
# repos: build_tools.repos

# mapping for package keys
conda_index:
  - conda-forge.yaml
  - packages-ignore.yaml

packages_skip_by_deps:

packages_select_by_deps:
  - rospy
  - roscpp

patch_dir: ./patch
